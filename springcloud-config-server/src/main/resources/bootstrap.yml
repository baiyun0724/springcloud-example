spring:
  application:
    name: springcloud-config-server
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/baiyun0724/springcloud-example}
          searchPaths: spring-config-repo

server:
  port: 8888